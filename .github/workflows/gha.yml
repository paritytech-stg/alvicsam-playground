name: self-hosted-runner-default
on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  merge_group:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  # actions: write
  # contents: read
  # checks: read

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  basic-info:
    # strategy:
    #   matrix:
    #     runner:
    #       [
    #         arc-runners-alvicsam,
    #         arc-runners-alvicsam-benchmark,
    #         arc-runners-beefy-stg,
    #       ]
    # runs-on: ${{ matrix.runner }}
    runs-on: arc-runners-alvicsam-benchmark
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: hello
        run: |
          sudo apt-get update && sudo apt-get install -y python3-pip python3
          pip3 install PyGithub
          sleep 600
          python3 stopper.py
  # default-runner:
  #   runs-on: arc-runners-alvicsam
  #   # runs-on: ubuntu-latest
  #   # container:
  #   #   image: ${{ vars.CI_UNIFIED_IMAGE }}
  #   steps:
  #     - name: hello
  #       run: |
  #         df -h
  #         lscpu
  # benchmark-runner:
  #   runs-on: arc-runners-alvicsam-benchmark
  #   # runs-on: ubuntu-latest
  #   # container:
  #   #   image: ${{ vars.CI_UNIFIED_IMAGE }}
  #   steps:
  #     - name: hello
  #       run: |
  #         df -h
  #         lscpu
  # hello-world-beefy:
  #   runs-on: arc-runners-beefy-stg
  #   steps:
  #     - name: hello
  #       run: echo hello
  # hello-world-alvicsam:
  #   runs-on: arc-runners-alvicsam
  #   steps:
  #     - name: hello
  #       run: echo hello
